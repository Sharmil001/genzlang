{
  "name": "genz-lang",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "web",
    "core"
  ],
  "scripts": {
    "dev:web": "bun run -C web dev",
    "dev:core": "bun run -C core dev",
    "build:web": "bun run -C web build",
    "build:core": "bun run -C core build",
    "lint": "biome lint",
    "typecheck:web": "tsc --noEmit -p web",
    "typecheck:be": "tsc --noEmit -p core"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "biome": "^0.3.3",
    "chevrotain": "^11.0.3"
  }
}
