{
  "name": "genz-lang",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:web": "cd web && yarn dev",
    "build:web": "cd web && yarn build",
    "dev:core": "cd core && bun run index.ts",
    "lint": "biome lint",
    "typecheck:web": "tsc --noEmit -p web",
    "typecheck:be": "tsc --noEmit -p core"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "biome": "^0.3.3",
    "chevrotain": "^11.0.3"
  }
}
